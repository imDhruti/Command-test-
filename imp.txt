git init
git config user.name (username)
git config user.email (email)

git add .(to add all files)
git add index.html(to add spacific file)

git status (to check uploaded file status (modified:   imp.txt if file is modified after --add .))

